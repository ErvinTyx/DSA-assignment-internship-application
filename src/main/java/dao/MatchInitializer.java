package dao;

import adt.ListInterface;
import control.ApplicantManager;
import control.CompanyManager;
import control.JobManager;
import control.MatchingEngine;
public class MatchInitializer {
    public static void matchStudents(MatchingEngine matchingEngine, JobManager jobManager, CompanyManager companyManager, ApplicantManager applicantManager) {
        // TODO: Enter new match data
        
    }
}
